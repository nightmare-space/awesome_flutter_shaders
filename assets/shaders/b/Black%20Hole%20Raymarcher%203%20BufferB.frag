{
  "sksl": {
    "entrypoint": "BlackHoleRaymarcher3BufferB_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec2 iResolution;\nuniform float iTime;\nuniform float iFrame;\nuniform vec4 iMouse;\nuniform vec4 iChannelWrap;\nuniform vec4 iChannelFilter;\nuniform vec2 iChannelResolution0;\nuniform vec2 iChannelResolution1;\nuniform vec2 iChannelResolution2;\nuniform vec2 iChannelResolution3;\nuniform shader iChannel0;\nuniform half2 iChannel0_size;\n\nvec4 fragColor;\n\nvec3 camPos;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nfloat FLT_flutter_local_hash(inout uint x)\n{\n    x = uint(int(x) * (-1640531535));\n    return fract(sin(float(x)) * 43758.546875);\n}\n\nfloat FLT_flutter_local_rand(vec3 position)\n{\n    uvec3 V = uvec3(floor(position + vec3(0.5)));\n    uint param = (V.x + (V.y * 65536)) + (V.z * 256);\n    float _108 = FLT_flutter_local_hash(param);\n    float h = _108;\n    return h;\n}\n\nfloat FLT_flutter_local_interpolate(vec3 position)\n{\n    vec3 quantPos = floor((position + vec3(0.5)) + vec3(0.5));\n    vec3 divPos = fract(position * 1.0);\n    vec3 param = quantPos + vec3(0.0);\n    vec3 param_1 = quantPos + vec3(1.0, 0.0, 0.0);\n    vec3 param_2 = quantPos + vec3(1.0, 1.0, 0.0);\n    vec3 param_3 = quantPos + vec3(0.0, 1.0, 0.0);\n    vec4 lerpXY = vec4(FLT_flutter_local_rand(param), FLT_flutter_local_rand(param_1), FLT_flutter_local_rand(param_2), FLT_flutter_local_rand(param_3));\n    vec3 param_4 = quantPos + vec3(0.0, 0.0, 1.0);\n    vec3 param_5 = quantPos + vec3(1.0, 0.0, 1.0);\n    vec3 param_6 = quantPos + vec3(1.0);\n    vec3 param_7 = quantPos + vec3(0.0, 1.0, 1.0);\n    vec4 lerpXYZ = vec4(FLT_flutter_local_rand(param_4), FLT_flutter_local_rand(param_5), FLT_flutter_local_rand(param_6), FLT_flutter_local_rand(param_7));\n    vec4 weights = vec4(abs((1.0 - divPos.x) * (1.0 - divPos.y)), abs((0.0 - divPos.x) * (1.0 - divPos.y)), abs((0.0 - divPos.x) * (0.0 - divPos.y)), abs((1.0 - divPos.x) * (0.0 - divPos.y)));\n    vec4 lerpFinal = mix(lerpXY, lerpXYZ, vec4(divPos.z));\n    return (((weights.x * lerpFinal.x) + (weights.y * lerpFinal.y)) + (weights.z * lerpFinal.z)) + (weights.w * lerpFinal.w);\n}\n\nfloat FLT_flutter_local_octave(vec3 coord, float octaves, float div)\n{\n    float col = 0.0;\n    float it = 1.0;\n    float cnt = 1.0;\n    for (int iter = 1; iter <= int(octaves); iter++)\n    {\n        vec3 param = (coord * it) / vec3(div);\n        col += (FLT_flutter_local_interpolate(param) / it);\n        it *= 1.89999997615814208984375;\n        cnt += (1.0 / it);\n    }\n    return pow(col / cnt, 1.0);\n}\n\nvec2 FLT_flutter_local_distField(vec3 position, vec3 origin)\n{\n    float radius = 45.0;\n    float dist = distance(origin, position);\n    float distXY = max(distance(origin.xy, position.xy) - 0.100000001490116119384765625, 0.0);\n    float fieldZ = max(0.0, pow(distance(origin.z, position.z), 2.5));\n    float angle = atan((position.x - origin.x) / (position.y - origin.y));\n    if (position.y <= origin.y)\n    {\n        angle += 3.141499996185302734375;\n    }\n    angle += (0.0500000007450580596923828125 * iTime);\n    float cloud = pow(clamp(radius / (dist - 0.100000001490116119384765625), 0.0, 1.0), 2.5);\n    float spiral = 0.0;\n    float occ = 0.0;\n    vec3 param = vec3(dist, 50.0 * (1.0 + sin(angle)), 1.0 * distance(origin.z + (3.0 * iTime), position.z));\n    float param_1 = 3.0;\n    float param_2 = 3.0;\n    spiral = FLT_flutter_local_octave(param, param_1, param_2);\n    float finalDF = cloud * clamp(spiral / fieldZ, 0.0, 1.0);\n    if (finalDF < 0.699999988079071044921875)\n    {\n        occ = 0.699999988079071044921875 - spiral;\n    }\n    return vec2(finalDF, max(occ / ((dist * distance(position.z, origin.z)) / 500.0), 0.0));\n}\n\nfloat FLT_flutter_local_starField(inout vec3 vector)\n{\n    float b = 0.0;\n    float sizeDiv = 500.0;\n    vector = (vec3(1.0) + normalize(vector)) * sizeDiv;\n    if (true)\n    {\n        for (int i = 0; i < 8; i++)\n        {\n            vec3 param = vec3(float(i));\n            vector += vec3((100.0 * FLT_flutter_local_rand(param)) / sizeDiv);\n            vec3 param_1 = vector;\n            float a = 1.0 - (4.0 * FLT_flutter_local_rand(param_1));\n            if (a < 0.89999997615814208984375)\n            {\n                a = 0.0;\n            }\n            a = 6.0 * pow(a, 20.0);\n            b += a;\n        }\n    }\n    return b / 8.0;\n}\n\nvec3 FLT_flutter_local_rayCast(vec2 rayAxis)\n{\n    float stepSize = 1.0;\n    float gravDis = 4024.36181640625;\n    float yTravel = camPos.y - 0.100000001490116119384765625;\n    float timeOff = (iTime + 12.0) / 15.0;\n    vec3 newCam = vec3(camPos.x, camPos.y + (cos(timeOff) * yTravel), sin(timeOff) * 30.0);\n    vec3 rayPos = newCam;\n    vec3 rayVel = vec3(cos(rayAxis.x), sin(rayAxis.x), sin(rayAxis.y));\n    float rayDist = distance(rayPos, vec3(0.0, 0.0, -0.0));\n    float rayVol = 0.0;\n    float colShift = 0.0;\n    float occ = 1.0;\n    rayPos += (rayVel * max(rayDist - 500.0, 0.0));\n    for (int i = 0; i <= 90; i++)\n    {\n        rayDist = max(distance(rayPos, vec3(0.0, 0.0, -0.0)) - 10.0, 0.001000000047497451305389404296875);\n        float boDist = pow(rayDist / 500.0, 2.0);\n        float diskDist = rayDist;\n        rayPos += rayVel;\n        vec3 rayDefl = normalize(vec3(0.0, 0.0, -0.0) - rayPos);\n        rayVel += (vec3(rayDefl.x * (1.0 / pow(rayDist, 4.0)), rayDefl.y * (1.0 / pow(rayDist, 4.0)), rayDefl.z * (1.0 / pow(rayDist, 4.0))) * (gravDis * pow(stepSize, 2.400000095367431640625)));\n        vec3 param = rayPos;\n        vec3 param_1 = vec3(0.0, 0.0, -0.0);\n        vec2 dField = FLT_flutter_local_distField(param, param_1);\n        stepSize = min(clamp(rayDist - 20.1000003814697265625, 0.0500000007450580596923828125, 20.0), max(boDist + distance(-0.0, rayPos.z), 0.20000000298023223876953125));\n        rayVel = normalize(rayVel) * stepSize;\n        occ += dField.y;\n        rayVol += (((dField.x * 8.0) * stepSize) / occ);\n        if (rayDist >= 1000.0)\n        {\n            vec3 param_2 = rayVel;\n            float _604 = FLT_flutter_local_starField(param_2);\n            return vec3((_604 / occ) + rayVol, clamp(rayVol * colShift, 0.0, 1.0), rayVol);\n        }\n        if (rayDist <= 0.100000001490116119384765625)\n        {\n            return vec3(rayVol, clamp(rayVol * colShift, 0.0, 1.0), rayVol);\n        }\n        colShift += ((rayVol * rayPos.x) / 9000.0);\n    }\n    if (rayDist >= 0.300000011920928955078125)\n    {\n        vec3 param_3 = rayVel;\n        float _645 = FLT_flutter_local_starField(param_3);\n        rayVol += (_645 / occ);\n    }\n    return vec3(rayVol, clamp(rayVol * colShift, 0.0, 1.0), rayVol);\n}\n\nvoid FLT_flutter_local_mainImage(out vec4 fragColor_1, vec2 fragCoord)\n{\n    vec2 uv = fragCoord / iResolution;\n    vec2 pixAxis = iChannel0.eval(iChannel0_size * ( uv)).xy;\n    vec2 param = pixAxis;\n    vec3 col = FLT_flutter_local_rayCast(param);\n    fragColor_1 = vec4(col, 1.0);\n}\n\nvoid FLT_main()\n{\n    camPos = vec3(0.0, 220.0, 0.0);\n    float keep = (((((((((((iFrame + iMouse.x) + iTime) + iResolution.x) + iChannelWrap.x) + iChannelResolution0.x) + iChannelResolution0.y) + iChannelResolution1.x) + iChannelResolution1.y) + iChannelResolution2.x) + iChannelResolution2.y) + iChannelResolution3.x) + iChannelResolution3.y;\n    vec2 fragCoord_1 = FLT_flutter_local_FlutterFragCoord();\n    vec2 param_2 = fragCoord_1;\n    vec4 param_1;\n    FLT_flutter_local_mainImage(param_1, param_2);\n    fragColor = param_1;\n    if (keep < (-100000002004087734272.0))\n    {\n        fragColor += vec4(keep);\n    }\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 0,
        "name": "iResolution",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "iTime",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 2,
        "name": "iFrame",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 3,
        "name": "iMouse",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 4,
        "name": "iChannelWrap",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 5,
        "name": "iChannelFilter",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 6,
        "name": "iChannelResolution0",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 7,
        "name": "iChannelResolution1",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 8,
        "name": "iChannelResolution2",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 9,
        "name": "iChannelResolution3",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 10,
        "name": "iChannel0",
        "rows": 1,
        "type": 12
      }
    ]
  }
}